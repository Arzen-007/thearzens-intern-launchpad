/**
 * Vercel catch-all serverless entry for the protected THE ARZENS dashboard API.
 * It only exposes server routes; secrets remain in Vercel Project Settings.
 */
export { default } from "../server/app";
